package com.dmrp.jpgrande.springboot.test.controladores;

import java.util.List;

import com.dmrp.jpgrande.springboot.test.modelo.entidades.Livro;
//import com.dmrp.jpgrande.springboot.test.modelo.repositorios.LivroRepository;

// TODO: Mover as funções para uma classe Service; Linkar ao JPA. 
public class LivroController {
    /* 
    private final LivroRepository repository;

    public LivroController(LivroRepository repository) {
        this.repository = repository;
    }

    List<Livro> listarLivros() {
        return (List<Livro>) repository.findAll();
    }

    Livro addLivro(Livro livro) {
        return repository.save(livro);
    }
    */ // Comentado por enquanto, só estou testando o usuário por agora.
    
}
